# 安全策略 · SECURITY

本仓库为**公开**仓库，请所有贡献者严格遵守。

## 绝不提交
- API Key / Token / 密码 / 私钥 / `.env` 文件 / 任何凭证。
- 用户个人数据、可识别隐私信息。

## 密钥如何处理
- 仅存使用者**本机浏览器**，或通过**服务端代理**间接调用第三方 AI 服务。
- 代码只引用环境变量占位符，真实值由部署环境注入。

## 万一泄露
1. 立即在对应平台**吊销 / 轮换**该密钥。
2. 从历史中移除（`git filter-repo` 或 GitHub 支持），并强制推送。
3. 在 `CHANGELOG.md` 记录事件与处置。

## 上报
发现漏洞或泄露，请通过私有渠道联系 Morgan，不要公开 issue 暴露细节。
